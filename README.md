1. Set up Azure Authentication:
   Log in to Azure using the Azure CLI by running "az login" command and following the instructions to authenticate.
   
3. Initialize Terraform:   
   "terraform init"
   
5. Plan Deployment:
   "terraform plan"
   
7. Apply Changes:
   "terraform apply"

   
   After testing or using the Terraform code, you can clean up the resources to avoid incurring unnecessary costs:
9. "terraform destroy"
  
